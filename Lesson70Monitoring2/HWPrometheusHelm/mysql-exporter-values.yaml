serviceMonitor:
  enabled: true
  additionalLabels:
    release: kube-prometheus-stack
  namespace: default
  endpoints:
    - port: metrics
      interval: 30s

mysql:
  host: "mysql-service"
  pass: "admin"
  user: "root"
